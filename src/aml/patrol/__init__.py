"""技能治理：让"装了一堆 skill"这件事不掉进版本黑洞。

三件事：
  1. **入库**：技能目录镜像进知识库 + 重建清单 + 增量灌向量库（技能内容也变成可检索的知识）
  2. **监控 + 自动更新**：比上游 commit，变了就自动更新**本地没改过**的技能
  3. **结尾播报**：把"这次新增/更新了什么"压成一句话，交给 agent 在回答结尾念给用户

安全底线（比功能重要）：有本地补丁（`local_patch`）或本地指纹与记录不符的技能**永不覆盖**，
只暂存到 `_pending/` 等人工复核；每次覆盖前都先备份到 `_backup/`。
"""

from .notify import NoticeQueue

__all__ = ["NoticeQueue"]
