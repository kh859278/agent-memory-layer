# 安全策略（Security Policy）

## 这个项目怎么理解"安全"

`aml` 是**本地优先**的工具：采集、嵌入、检索都在本机；唯一的出网点是"蒸馏"（把会话摘要发给
你指定的 LLM），而它在发送前会**脱敏**（规则见 `src/aml/redact.py`，与提交前的泄漏扫描同一份）。

它同时做两件需要谨慎的事，请你在采用前知道边界：

1. **它会把库里的文本注入 agent 的上下文**（检索召回）。程序性内容（技能正文）默认不参与检索，
   但一段被污染的"知识"仍可能被召回 —— 治理方式是"来源可见 + 人写的优先 + 复核期"，不是写入侧审批
   （见 `docs/TRUST-MODEL.md` 的"刻意不做"）。
2. **它会更新你自己的技能目录**（`aml patrol`）。覆盖前一律备份，本地有改动只暂存；
   "要不要批准凭据"是配置策略（默认要求）。

## 报告漏洞

- 优先用 GitHub 的[私密漏洞报告](https://docs.github.com/en/code-security/security-advisories/guidance-on-reporting-and-writing-information-about-vulnerabilities/privately-reporting-a-security-vulnerability)
  （仓库页 → **Security** → **Report a vulnerability**）。**请不要**为未修复的问题开公开 issue。
- 不方便用 GitHub 时，开一个**不含细节**的 issue 说明你要私下联系，维护者会回你一个私密渠道。
- 请附：受影响的版本或 commit、复现步骤、影响范围（能读什么/能改什么）、以及你希望怎么修。
  有 PoC 就给，**别在报告里带上真实密钥或他人隐私数据**。

## 我们在意什么（按优先级）

1. **能让别人拿到你的数据**：本机路径/用户名/会话内容被写进公开仓库或发到外部端点。
2. **能让别人的代码在你的机器上执行**：安装/更新路径、解包、以及"把上游内容当指令"的那些环节。
3. **能让 agent 被诱导做出你没让它做的事**：提示注入 → 持久化进记忆或技能。

**不在范围内**（不是缺陷）：记忆服务后端本身没有鉴权（它只绑 `127.0.0.1`，本仓库不提供访问控制，
只提供 `memory_api_token` 客户端支持）；把 `AML_HOME` 指到公开目录；自己把密钥写进配置并提交。

## 支持范围

- 只维护**最新发布版**与 `main`。alpha 阶段不承诺回移修复。
- 报告确认后：能修的出补丁并在 release notes 里写明；不能立刻修的，写进 `ROADMAP.md`
  并在 issue 里说明处置（**低报比不报更糟**，所以我们倾向把"已知没防住"写清楚）。
